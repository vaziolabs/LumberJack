#pragma once
#include "key.h"
#include "keynode.h"

/*
class KeyTree {
public:
	KeyNode* root;

	KeyTree() : root(nullptr) {}
	KeyTree(KeyNode* root) : root(root) {}

	KeyNode* insert(Key *key);
	KeyNode* search(Key *key);
	void remove(Key* key);

	//void print();
};
*/